#include <iostream>
using namespace std;

int main(){
    //we use int command indivisually
    /*int a = 6;
    int b = 10;*/

    //and we can combine the variables of same class
    int a = 6 , b = 20;

    // Now we use char command 
    char le = 'a';

    // Now we use floot command
    float pi = 3.14;

    cout <<"This is class about scope. "<<"And we print the value of a "<<a <<".\n And the value of b "<<b<<". the value of letter le is "<<le<<"\n The value of pi is "<<pi;
     
    return 0;
}